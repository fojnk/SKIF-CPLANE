"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getColumnTitle = exports.isEnabledHiding = exports.getIsVisible = void 0;
const getIsVisible = (column, visibilityState) => {
    if (column.columns.length)
        return column.columns.some((innerColumn) => (0, exports.getIsVisible)(innerColumn, visibilityState));
    return visibilityState[column.id] ?? true;
};
exports.getIsVisible = getIsVisible;
const isEnabledHiding = (column) => column.columnDef.enableHiding ?? true;
exports.isEnabledHiding = isEnabledHiding;
const getColumnTitle = (column, header) => {
    const { columnDef } = column;
    if (columnDef.meta?.titleInSettings) {
        const titleInSettings = columnDef.meta?.titleInSettings;
        return typeof titleInSettings === 'function' ? titleInSettings(column) : titleInSettings;
    }
    const columnHeader = columnDef.header;
    return typeof columnHeader === 'function' ? columnHeader(header?.getContext?.()) : columnHeader;
};
exports.getColumnTitle = getColumnTitle;
//# sourceMappingURL=TableSettingsColumn.utils.js.map
