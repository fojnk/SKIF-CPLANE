import type { Header, VisibilityState } from '@tanstack/react-table';
import type { Column } from "../../types/base.js";
export declare const getIsVisible: <TData extends unknown>(column: Column<TData>, visibilityState: VisibilityState) => boolean;
export declare const isEnabledHiding: <TData extends unknown>(column: Column<TData>) => boolean;
export declare const getColumnTitle: <TData extends unknown>(column: Column<TData, unknown>, header: Header<TData, unknown>) => any;
