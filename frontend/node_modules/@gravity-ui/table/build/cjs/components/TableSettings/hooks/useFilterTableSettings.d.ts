import type { Column, Header } from "../../../types/base.js";
interface UseFilterTableSettingsParams<TData> {
    filteredColumns: Column<TData, unknown>[];
    search: string;
    expandNestedColumns: boolean;
    headersById: Record<string, Header<TData, unknown>>;
}
export declare const useFilterTableSettings: <TData>({ filteredColumns, search, expandNestedColumns, headersById, }: UseFilterTableSettingsParams<TData>) => {
    hiddenNodes: Set<string>;
    partiallyHiddenNodes: Set<string>;
};
export {};
