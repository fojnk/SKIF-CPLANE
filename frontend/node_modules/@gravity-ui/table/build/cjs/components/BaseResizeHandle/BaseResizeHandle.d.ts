import type { Header } from "../../types/base.js";
import "./BaseResizeHandle.css";
export interface BaseResizeHandleProps<TData, TValue> {
    className?: string;
    header: Header<TData, TValue>;
}
export declare const BaseResizeHandle: <TData, TValue>({ className, header, }: BaseResizeHandleProps<TData, TValue>) => import("react/jsx-runtime").JSX.Element;
