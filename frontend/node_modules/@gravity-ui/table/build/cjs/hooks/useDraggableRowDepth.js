"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.useDraggableRowDepth = void 0;
const React = __importStar(require("react"));
const components_1 = require("../components/index.js");
const useDraggableRowDepth = ({ row, table, isDragging, }) => {
    const { isChildMode, isParentMode, isNextChildMode, targetItemIndex = -1, enableNesting, } = React.useContext(components_1.SortableListContext) ?? {};
    let isFirstChild = isDragging && targetItemIndex === -1;
    let depth = 0;
    if (enableNesting) {
        if (isDragging && targetItemIndex !== -1) {
            const rows = table.getRowModel().rows;
            const targetItemDepth = rows[targetItemIndex]?.depth ?? 0;
            const nextItemDepth = rows[targetItemIndex + 1]?.depth ?? 0;
            isFirstChild = nextItemDepth > targetItemDepth;
            if (isFirstChild) {
                depth = nextItemDepth;
                if (isParentMode) {
                    depth--;
                }
            }
            else {
                const baseDepth = isNextChildMode
                    ? targetItemDepth
                    : Math.min(targetItemDepth, nextItemDepth);
                let childDepthOffset = 0;
                if (isParentMode) {
                    childDepthOffset = -1;
                }
                else if (isChildMode) {
                    childDepthOffset = 1;
                }
                depth = baseDepth + childDepthOffset;
            }
            depth = Math.max(0, depth);
        }
        else {
            depth = isFirstChild ? 0 : row.depth;
        }
    }
    return {
        depth,
        isFirstChild,
    };
};
exports.useDraggableRowDepth = useDraggableRowDepth;
//# sourceMappingURL=useDraggableRowDepth.js.map
