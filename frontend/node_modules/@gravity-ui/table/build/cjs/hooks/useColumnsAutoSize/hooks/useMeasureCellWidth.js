"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.useMeasureCellWidth = useMeasureCellWidth;
const React = __importStar(require("react"));
const client_1 = require("react-dom/client");
const constants_1 = require("../constants.js");
const renderElementForMeasure_1 = require("../utils/renderElementForMeasure.js");
function useMeasureCellWidth({ renderElementForMeasure = renderElementForMeasure_1.renderElementForMeasure, }) {
    const rootRef = React.useRef(null);
    const measureContainerRef = React.useRef(null);
    const lastMeasuredElementRef = React.useRef(null);
    React.useEffect(() => {
        return () => {
            if (rootRef.current) {
                rootRef.current.unmount();
                rootRef.current = null;
            }
            if (measureContainerRef.current) {
                document.body.removeChild(measureContainerRef.current);
                measureContainerRef.current = null;
            }
        };
    }, []);
    return React.useCallback((element, cellType = 'cell') => {
        if (element === null || element === undefined) {
            return 0;
        }
        if (!measureContainerRef.current) {
            const container = document.createElement('div');
            container.style.position = 'absolute';
            container.style.visibility = 'hidden';
            container.style.left = '-9999px';
            container.style.top = '-9999px';
            container.style.width = 'auto';
            container.style.display = 'inline-block';
            document.body.appendChild(container);
            measureContainerRef.current = container;
            rootRef.current = (0, client_1.createRoot)(container);
        }
        if (lastMeasuredElementRef.current &&
            lastMeasuredElementRef.current.element === element) {
            return lastMeasuredElementRef.current.width;
        }
        if (typeof element === 'string' ||
            typeof element === 'number' ||
            typeof element === 'boolean') {
            const text = String(element);
            if (text.trim() === '') {
                return 0;
            }
            const tempElement = document.createElement('div');
            tempElement.style.whiteSpace = 'nowrap';
            tempElement.style.display = 'inline-block';
            tempElement.style.visibility = 'hidden';
            tempElement.style.position = 'static';
            tempElement.style.fontWeight = cellType === 'header' ? 'bold' : 'normal';
            tempElement.textContent = text;
            measureContainerRef.current?.appendChild(tempElement);
            const width = tempElement.getBoundingClientRect().width;
            measureContainerRef.current?.removeChild(tempElement);
            lastMeasuredElementRef.current = { element, width };
            return width;
        }
        try {
            rootRef.current.render(renderElementForMeasure(element));
            return new Promise((resolve) => {
                setTimeout(() => {
                    try {
                        const container = measureContainerRef.current;
                        const width = container?.getBoundingClientRect().width ?? 0;
                        if (width === 0) {
                            const defaultWidth = cellType === 'header' ? constants_1.headerDefaultWidth : constants_1.cellDefaultWidth;
                            lastMeasuredElementRef.current = { element, width: defaultWidth };
                            resolve(defaultWidth);
                        }
                        else {
                            lastMeasuredElementRef.current = { element, width };
                            resolve(width);
                        }
                    }
                    catch {
                        const defaultWidth = cellType === 'header' ? constants_1.headerDefaultWidth : constants_1.cellDefaultWidth;
                        lastMeasuredElementRef.current = { element, width: defaultWidth };
                        resolve(defaultWidth);
                    }
                }, 0);
            });
        }
        catch {
            const defaultWidth = cellType === 'header' ? constants_1.headerDefaultWidth : constants_1.cellDefaultWidth;
            lastMeasuredElementRef.current = { element, width: defaultWidth };
            return defaultWidth;
        }
    }, [renderElementForMeasure]);
}
//# sourceMappingURL=useMeasureCellWidth.js.map
