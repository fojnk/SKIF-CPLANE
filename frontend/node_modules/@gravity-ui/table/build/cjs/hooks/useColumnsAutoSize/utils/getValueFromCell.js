"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.getValueFromCell = getValueFromCell;
const React = __importStar(require("react"));
const react_table_1 = require("@tanstack/react-table");
function getValueFromCell(tableInstance, column, row) {
    try {
        if (column.cell) {
            const getValue = () => {
                if ('accessorKey' in column) {
                    return row.original[column.accessorKey];
                }
                if ('accessorFn' in column) {
                    return column.accessorFn(row.original, 0);
                }
                return undefined;
            };
            const context = {
                table: tableInstance || {},
                row,
                column: column,
                getValue,
                renderValue: () => getValue(),
            };
            return (0, react_table_1.flexRender)(column.cell, context);
        }
        let value;
        if ('accessorKey' in column) {
            value = row.original[column.accessorKey];
        }
        else if ('accessorFn' in column) {
            value = column.accessorFn(row.original, 0);
        }
        else {
            return '';
        }
        if (value !== null &&
            value !== undefined &&
            typeof value === 'object' &&
            !React.isValidElement(value)) {
            try {
                if (value instanceof Date) {
                    return value.toLocaleString();
                }
                const stringified = JSON.stringify(value);
                if (stringified.length > 30) {
                    return stringified.slice(0, 27) + '...';
                }
                return stringified;
            }
            catch {
                return '[Object]';
            }
        }
        return value;
    }
    catch {
        return '';
    }
}
//# sourceMappingURL=getValueFromCell.js.map
