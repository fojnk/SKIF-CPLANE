"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.useColumnsAutoSize = useColumnsAutoSize;
const React = __importStar(require("react"));
const debounce_js_1 = __importDefault(require("lodash/debounce.js"));
const react_use_1 = require("react-use");
const constants_1 = require("./constants.js");
const useMeasureCellWidth_1 = require("./hooks/useMeasureCellWidth.js");
const getValueFromCell_1 = require("./utils/getValueFromCell.js");
const hasDefinedWidth_1 = require("./utils/hasDefinedWidth.js");
const renderHeaderContent_1 = require("./utils/renderHeaderContent.js");
const setColumnAutoSizes_1 = require("./utils/setColumnAutoSizes.js");
function useColumnsAutoSize({ columns, options, renderElementForMeasure, }) {
    const [columnWidths, setColumnWidths] = React.useState({});
    const [isMeasuring, setIsMeasuring] = React.useState(true);
    const [tableInstance, setTableInstance] = React.useState(null);
    const rows = tableInstance?.getRowModel().rows ?? constants_1.emptyRows;
    const sampledRows = rows.slice(0, options?.sampleSize ?? 100);
    const columnSizing = tableInstance?.getState().columnSizing ?? constants_1.emptyColumnSizing;
    const measureCellWidth = (0, useMeasureCellWidth_1.useMeasureCellWidth)({
        renderElementForMeasure,
    });
    const calculateWidths = React.useMemo(() => (0, debounce_js_1.default)(async ({ columnSizing, columns, measureCellWidth, sampledRows, tableInstance, minWidth = 50, maxWidth = 500, padding = 16, headerPadding = 24, measureHeaderText = true, respectExistingWidths = true, respectResizedWidths = true, }) => {
        const newWidths = {};
        const userResizedColumns = respectResizedWidths ? columnSizing : {};
        for (const column of columns) {
            const id = column.id ||
                ('accessorKey' in column && String(column.accessorKey)) ||
                '';
            if (!id)
                continue;
            if (respectResizedWidths && userResizedColumns[id]) {
                newWidths[id] = userResizedColumns[id];
                continue;
            }
            if (respectExistingWidths && (0, hasDefinedWidth_1.hasDefinedWidth)(column)) {
                if (typeof column.size === 'number') {
                    newWidths[id] = column.size;
                }
                else if (typeof column.minSize === 'number' &&
                    typeof column.maxSize === 'number' &&
                    column.minSize === column.maxSize) {
                    newWidths[id] = column.minSize;
                }
                continue;
            }
            let maxContentWidth = 0;
            if (measureHeaderText && column.header) {
                try {
                    const headerContent = await (0, renderHeaderContent_1.renderHeaderContent)(tableInstance, column);
                    const headerWidth = await measureCellWidth(headerContent, 'header');
                    if (headerWidth === 0) {
                        maxContentWidth = constants_1.headerDefaultWidth + headerPadding;
                    }
                    else {
                        maxContentWidth = headerWidth + headerPadding;
                    }
                }
                catch {
                    maxContentWidth = constants_1.headerDefaultWidth + headerPadding;
                }
            }
            for (const row of sampledRows) {
                try {
                    const cellValue = (0, getValueFromCell_1.getValueFromCell)(tableInstance, column, row);
                    if (cellValue === null || cellValue === undefined) {
                        continue;
                    }
                    const cellWidth = await measureCellWidth(cellValue, 'cell');
                    maxContentWidth = Math.max(maxContentWidth, cellWidth + padding);
                }
                catch { }
            }
            if (maxContentWidth <= padding) {
                maxContentWidth = constants_1.cellDefaultWidth;
            }
            newWidths[id] = Math.min(Math.max(maxContentWidth, minWidth), maxWidth);
        }
        setColumnWidths(newWidths);
        setIsMeasuring(false);
    }, 100), []);
    (0, react_use_1.useDeepCompareEffect)(() => {
        if (!sampledRows.length) {
            return;
        }
        calculateWidths.cancel();
        calculateWidths({
            columnSizing,
            columns,
            measureCellWidth,
            sampledRows,
            tableInstance,
            ...options,
        });
    }, [columnSizing, sampledRows]);
    const columnsWithAutoSizes = React.useMemo(() => (0, setColumnAutoSizes_1.setColumnAutoSizes)(columns, columnWidths, columnSizing), [columns, columnWidths, columnSizing]);
    return { columnWidths, setTableInstance, columnsWithAutoSizes, isMeasuring };
}
//# sourceMappingURL=useColumnsAutoSize.js.map
