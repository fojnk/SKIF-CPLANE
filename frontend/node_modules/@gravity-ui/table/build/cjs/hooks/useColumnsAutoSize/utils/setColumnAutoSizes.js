"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.setColumnAutoSizes = setColumnAutoSizes;
const hasDefinedWidth_1 = require("./hasDefinedWidth.js");
function setColumnAutoSizes(columns, columnWidths, columnSizing) {
    if (!Object.keys(columnWidths).length) {
        return columns;
    }
    return columns.map((column) => {
        const id = column.id || ('accessorKey' in column && String(column.accessorKey)) || '';
        if (!id)
            return column;
        const hasUserResized = Object.keys(columnSizing).length > 0 && columnSizing[id];
        if (hasUserResized) {
            return {
                ...column,
                size: columnSizing[id],
            };
        }
        if (columnWidths[id] && !(0, hasDefinedWidth_1.hasDefinedWidth)(column)) {
            return {
                ...column,
                size: columnWidths[id],
                minSize: columnWidths[id],
            };
        }
        return column;
    });
}
//# sourceMappingURL=setColumnAutoSizes.js.map
