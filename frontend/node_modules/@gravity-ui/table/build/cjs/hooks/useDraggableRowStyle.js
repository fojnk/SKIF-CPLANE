"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.useDraggableRowStyle = void 0;
const React = __importStar(require("react"));
const components_1 = require("../components/index.js");
const defaultStyle = {};
const useDraggableRowStyle = ({ style = defaultStyle, transform, transition, isDragging, isDragActive, isFirstChild, draggableChildRowOffset = 32, enableNesting, }) => {
    const { isChildMode, isParentMode } = React.useContext(components_1.SortableListContext) ?? {};
    return React.useMemo(() => {
        if (!isDragActive || !transform) {
            return style;
        }
        let x = 0;
        if (enableNesting && isDragging) {
            if (isParentMode) {
                x = -draggableChildRowOffset;
            }
            else if (isChildMode && !isFirstChild) {
                x = draggableChildRowOffset;
            }
        }
        return {
            ...style,
            transition,
            transform: `translate3d(${Math.max(x, 0)}px, ${transform.y}px, 0)`,
        };
    }, [
        draggableChildRowOffset,
        isChildMode,
        isDragActive,
        isDragging,
        isFirstChild,
        isParentMode,
        style,
        transform,
        transition,
        enableNesting,
    ]);
};
exports.useDraggableRowStyle = useDraggableRowStyle;
//# sourceMappingURL=useDraggableRowStyle.js.map
