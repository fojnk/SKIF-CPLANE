import * as React from 'react';
import type { CellContext, RowData } from '@tanstack/react-table';
export declare const useToggleRangeSelectionHandler: <TData extends RowData, TValue>(cellContext: CellContext<TData, TValue>) => (event: React.ChangeEvent<HTMLInputElement>) => void;
