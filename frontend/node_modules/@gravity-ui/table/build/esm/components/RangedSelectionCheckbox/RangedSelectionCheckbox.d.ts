import * as React from 'react';
import type { CheckboxProps } from '@gravity-ui/uikit';
import type { CellContext, RowData } from '@tanstack/react-table';
export type RangedSelectionCheckboxProps<TData extends RowData, TValue> = Omit<CheckboxProps, 'onChange'> & {
    cellContext: CellContext<TData, TValue>;
};
export declare const RangedSelectionCheckbox: <TData extends RowData, TValue>(props: RangedSelectionCheckboxProps<TData, TValue> & {
    ref?: React.Ref<HTMLLabelElement>;
}) => React.ReactElement;
