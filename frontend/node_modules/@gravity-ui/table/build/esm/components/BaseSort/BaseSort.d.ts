import type * as React from 'react';
import type { Header } from "../../types/base.js";
export interface BaseSortProps<TData, TValue> extends Omit<React.HTMLAttributes<HTMLSpanElement>, 'className' | 'onClick'> {
    className?: string;
    header: Header<TData, TValue>;
}
export declare const BaseSort: <TData, TValue>({ header, className, children, ...restProps }: BaseSortProps<TData, TValue>) => import("react/jsx-runtime").JSX.Element;
