import { jsx as _jsx } from "react/jsx-runtime";
import { shouldRenderHeaderCell } from "../../utils/index.js";
import { BaseHeaderCell } from "../BaseHeaderCell/index.js";
import { b } from "../BaseTable/BaseTable.classname.js";
export const BaseHeaderRow = ({ cellClassName, className: classNameProp, headerGroup, parentHeaderGroup, renderHeaderCellContent, renderResizeHandle, renderSortIndicator, resizeHandleClassName, sortIndicatorClassName, attributes: attributesProp, cellAttributes, ...restProps }) => {
    const attributes = typeof attributesProp === 'function'
        ? attributesProp(headerGroup, parentHeaderGroup)
        : attributesProp;
    const className = typeof classNameProp === 'function'
        ? classNameProp(headerGroup, parentHeaderGroup)
        : classNameProp;
    return (_jsx("tr", { className: b('header-row', className), ...restProps, ...attributes, children: headerGroup.headers.map((header) => {
            const parentHeader = parentHeaderGroup?.headers.find((item) => header.column.id === item.column.id);
            return shouldRenderHeaderCell(header, parentHeader) ? (_jsx(BaseHeaderCell, { className: cellClassName, header: header, parentHeader: parentHeader, renderHeaderCellContent: renderHeaderCellContent, renderResizeHandle: renderResizeHandle, renderSortIndicator: renderSortIndicator, resizeHandleClassName: resizeHandleClassName, sortIndicatorClassName: sortIndicatorClassName, attributes: cellAttributes }, header.column.id)) : null;
        }) }));
};
//# sourceMappingURL=BaseHeaderRow.js.map
