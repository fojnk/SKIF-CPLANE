import type { TableActionConfig, TableActionGroup } from "../../types/RowActions.js";
export declare const isActionGroup: <TValue extends unknown>(config: TableActionConfig<TValue>) => config is TableActionGroup<TValue>;
