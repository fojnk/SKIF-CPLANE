import { jsx as _jsx } from "react/jsx-runtime";
import { Menu } from '@gravity-ui/uikit';
import { isActionGroup } from "./RowActionsMenu.utils.js";
export const RowActionsMenu = ({ item, actions, size, onMenuItemClick, className, itemClassName, }) => {
    const renderPopupMenuItem = (action, index) => {
        if (isActionGroup(action)) {
            return (_jsx(Menu.Group, { label: action.title, children: action.items.map(renderPopupMenuItem) }, index));
        }
        const { text, icon, handler, href, ...restProps } = action;
        const handleMenuItemClick = (event) => {
            event.stopPropagation();
            handler(item, index, event);
            onMenuItemClick();
        };
        return (_jsx(Menu.Item, { onClick: handleMenuItemClick, href: typeof href === 'function' ? href(item, index) : href, iconStart: icon, className: itemClassName, ...restProps, children: text }, index));
    };
    return (_jsx(Menu, { className: className, size: size, children: actions.map(renderPopupMenuItem) }));
};
//# sourceMappingURL=RowActionsMenu.js.map
