import { jsx as _jsx } from "react/jsx-runtime";
import { b } from "./BaseResizeHandle.classname.js";
import "./BaseResizeHandle.css";
export const BaseResizeHandle = ({ className, header, }) => {
    const { table } = header.getContext();
    const { columnResizeDirection, columnResizeMode } = table.options;
    const { columnSizingInfo } = table.getState();
    const offset = (columnResizeDirection === 'rtl' ? -1 : 1) * (columnSizingInfo.deltaOffset ?? 0);
    return (
    // eslint-disable-next-line jsx-a11y/no-static-element-interactions
    _jsx("div", { className: b({
            direction: columnResizeDirection,
            resizing: header.column.getIsResizing(),
        }, className), onDoubleClick: () => header.column.resetSize(), onMouseDown: header.getResizeHandler(), onTouchStart: header.getResizeHandler(), style: {
            transform: columnResizeMode === 'onEnd' && header.column.getIsResizing()
                ? `translateX(${offset}px)`
                : undefined,
        } }));
};
//# sourceMappingURL=BaseResizeHandle.js.map
