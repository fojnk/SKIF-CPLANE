import { jsx as _jsx } from "react/jsx-runtime";
import * as React from 'react';
import { SortableContext, useSortable, verticalListSortingStrategy } from '@dnd-kit/sortable';
import { useSortableList } from "../../hooks/index.js";
import { SortableListContext } from "../SortableListContext/index.js";
export const SortableList = ({ children, items, onDragStart, onDragEnd, enableNesting, }) => {
    const { activeItemKey, activeItemIndex, isChildMode, isParentMode, isNextChildMode, targetItemIndex, } = useSortableList({
        items,
        onDragStart,
        onDragEnd,
        enableNesting,
    });
    const contextValue = React.useMemo(() => ({
        activeItemKey,
        activeItemIndex,
        isChildMode,
        isNextChildMode,
        isParentMode,
        targetItemIndex,
        enableNesting,
        useSortable,
    }), 
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [activeItemKey, isChildMode, isNextChildMode, isParentMode, targetItemIndex, enableNesting]);
    return (_jsx(SortableListContext.Provider, { value: contextValue, children: _jsx(SortableContext, { items: items, strategy: verticalListSortingStrategy, children: children }) }));
};
//# sourceMappingURL=SortableList.js.map
