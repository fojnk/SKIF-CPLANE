import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import * as React from 'react';
import { Ellipsis } from '@gravity-ui/icons';
import { Button, Icon, Popup, useUniqId } from '@gravity-ui/uikit';
import { RowActionsMenu } from "../RowActionsMenu/index.js";
import { b } from "./RowActions.classname.js";
import i18n from "./i18n/index.js";
import "./RowActions.css";
const POPUP_PLACEMENT = ['bottom-end', 'top-end', 'left', 'right'];
export const RowActions = ({ index: rowIndex, item, getRowActions, rowActionsSize, }) => {
    const [isPopupOpen, setIsPopupOpen] = React.useState(false);
    const anchorRef = React.useRef(null);
    const rowId = useUniqId();
    const buttonExtraProps = React.useMemo(() => ({
        'aria-label': i18n('label-actions'),
        'aria-expanded': isPopupOpen,
        'aria-controls': rowId,
    }), [isPopupOpen, rowId]);
    const handleButtonClick = React.useCallback((event) => {
        setIsPopupOpen((value) => !value);
        event.stopPropagation();
    }, []);
    if (getRowActions === undefined) {
        return null;
    }
    const actions = getRowActions(item, rowIndex);
    if (actions.length === 0) {
        return null;
    }
    return (_jsxs("div", { className: b(), children: [_jsx(Popup, { open: isPopupOpen, anchorRef: anchorRef, placement: POPUP_PLACEMENT, onOutsideClick: () => setIsPopupOpen(false), id: rowId, children: _jsx("div", { onClick: (event) => {
                        event.stopPropagation();
                    }, children: _jsx(RowActionsMenu, { item: item, actions: actions, size: rowActionsSize, onMenuItemClick: () => setIsPopupOpen(false), className: b('popup-menu'), itemClassName: b('popup-menu-item') }) }) }), _jsx(Button, { view: "flat-secondary", className: b('actions-button'), onClick: handleButtonClick, size: rowActionsSize, ref: anchorRef, extraProps: buttonExtraProps, children: _jsx(Icon, { data: Ellipsis }) })] }));
};
//# sourceMappingURL=RowActions.js.map
