import { jsx as _jsx } from "react/jsx-runtime";
import { DndContext, MeasuringStrategy, PointerSensor, rectIntersection, useSensor, useSensors, } from '@dnd-kit/core';
const measuring = {
    droppable: {
        strategy: MeasuringStrategy.WhileDragging,
    },
};
export const SortableListDndContext = ({ children, modifiers }) => {
    const pointerSensor = useSensor(PointerSensor);
    const sensors = useSensors(pointerSensor);
    return (_jsx(DndContext, { sensors: sensors, collisionDetection: rectIntersection, measuring: measuring, modifiers: modifiers, children: children }));
};
//# sourceMappingURL=SortableListDndContext.js.map
