import { ArrowDown as ArrowDownIcon, ArrowUpArrowDown as ArrowUpArrowDownIcon, ArrowUp as ArrowUpIcon, } from '@gravity-ui/icons';
export const getSortIndicatorIcon = (sortDirection) => {
    if (sortDirection === 'asc') {
        return ArrowUpIcon;
    }
    if (sortDirection === 'desc') {
        return ArrowDownIcon;
    }
    return ArrowUpArrowDownIcon;
};
//# sourceMappingURL=getSortIndicatorIcon.js.map
