export const shouldRenderHeaderCell = (header, parentHeader) => {
    const isPlaceholderRowSpannedCell = header.isPlaceholder &&
        parentHeader?.isPlaceholder &&
        parentHeader.placeholderId === header.placeholderId;
    const isLeafRowSpannedCell = !header.isPlaceholder &&
        header.id === header.column.id &&
        header.depth - header.column.depth > 1;
    return !(isPlaceholderRowSpannedCell || isLeafRowSpannedCell);
};
//# sourceMappingURL=shouldRenderHeaderCell.js.map
