import * as React from 'react';
import { SortableListContext } from "../components/index.js";
const defaultStyle = {};
export const useDraggableRowStyle = ({ style = defaultStyle, transform, transition, isDragging, isDragActive, isFirstChild, draggableChildRowOffset = 32, enableNesting, }) => {
    const { isChildMode, isParentMode } = React.useContext(SortableListContext) ?? {};
    return React.useMemo(() => {
        if (!isDragActive || !transform) {
            return style;
        }
        let x = 0;
        if (enableNesting && isDragging) {
            if (isParentMode) {
                x = -draggableChildRowOffset;
            }
            else if (isChildMode && !isFirstChild) {
                x = draggableChildRowOffset;
            }
        }
        return {
            ...style,
            transition,
            transform: `translate3d(${Math.max(x, 0)}px, ${transform.y}px, 0)`,
        };
    }, [
        draggableChildRowOffset,
        isChildMode,
        isDragActive,
        isDragging,
        isFirstChild,
        isParentMode,
        style,
        transform,
        transition,
        enableNesting,
    ]);
};
//# sourceMappingURL=useDraggableRowStyle.js.map
