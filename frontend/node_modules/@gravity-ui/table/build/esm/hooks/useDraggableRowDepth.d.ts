import type { Row, Table } from '@tanstack/react-table';
export interface UseDraggableRowDepthOptions<TData> {
    /** Row object */
    row: Row<TData>;
    /** The table instance returned from the `useTable` hook */
    table: Table<TData>;
    /** Determines whether the row is being dragged */
    isDragging?: boolean;
}
export declare const useDraggableRowDepth: <TData>({ row, table, isDragging, }: UseDraggableRowDepthOptions<TData>) => {
    depth: number;
    isFirstChild: boolean | undefined;
};
