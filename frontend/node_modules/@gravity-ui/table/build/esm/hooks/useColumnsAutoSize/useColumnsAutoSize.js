import * as React from 'react';
import debounce from 'lodash/debounce.js';
import { useDeepCompareEffect } from 'react-use';
import { cellDefaultWidth, emptyColumnSizing, emptyRows, headerDefaultWidth } from "./constants.js";
import { useMeasureCellWidth } from "./hooks/useMeasureCellWidth.js";
import { getValueFromCell } from "./utils/getValueFromCell.js";
import { hasDefinedWidth } from "./utils/hasDefinedWidth.js";
import { renderHeaderContent } from "./utils/renderHeaderContent.js";
import { setColumnAutoSizes } from "./utils/setColumnAutoSizes.js";
export function useColumnsAutoSize({ columns, options, renderElementForMeasure, }) {
    const [columnWidths, setColumnWidths] = React.useState({});
    const [isMeasuring, setIsMeasuring] = React.useState(true);
    const [tableInstance, setTableInstance] = React.useState(null);
    const rows = tableInstance?.getRowModel().rows ?? emptyRows;
    const sampledRows = rows.slice(0, options?.sampleSize ?? 100);
    const columnSizing = tableInstance?.getState().columnSizing ?? emptyColumnSizing;
    const measureCellWidth = useMeasureCellWidth({
        renderElementForMeasure,
    });
    const calculateWidths = React.useMemo(() => debounce(async ({ columnSizing, columns, measureCellWidth, sampledRows, tableInstance, minWidth = 50, maxWidth = 500, padding = 16, headerPadding = 24, measureHeaderText = true, respectExistingWidths = true, respectResizedWidths = true, }) => {
        const newWidths = {};
        const userResizedColumns = respectResizedWidths ? columnSizing : {};
        for (const column of columns) {
            const id = column.id ||
                ('accessorKey' in column && String(column.accessorKey)) ||
                '';
            if (!id)
                continue;
            if (respectResizedWidths && userResizedColumns[id]) {
                newWidths[id] = userResizedColumns[id];
                continue;
            }
            if (respectExistingWidths && hasDefinedWidth(column)) {
                if (typeof column.size === 'number') {
                    newWidths[id] = column.size;
                }
                else if (typeof column.minSize === 'number' &&
                    typeof column.maxSize === 'number' &&
                    column.minSize === column.maxSize) {
                    newWidths[id] = column.minSize;
                }
                continue;
            }
            let maxContentWidth = 0;
            if (measureHeaderText && column.header) {
                try {
                    const headerContent = await renderHeaderContent(tableInstance, column);
                    const headerWidth = await measureCellWidth(headerContent, 'header');
                    if (headerWidth === 0) {
                        maxContentWidth = headerDefaultWidth + headerPadding;
                    }
                    else {
                        maxContentWidth = headerWidth + headerPadding;
                    }
                }
                catch {
                    maxContentWidth = headerDefaultWidth + headerPadding;
                }
            }
            for (const row of sampledRows) {
                try {
                    const cellValue = getValueFromCell(tableInstance, column, row);
                    if (cellValue === null || cellValue === undefined) {
                        continue;
                    }
                    const cellWidth = await measureCellWidth(cellValue, 'cell');
                    maxContentWidth = Math.max(maxContentWidth, cellWidth + padding);
                }
                catch { }
            }
            if (maxContentWidth <= padding) {
                maxContentWidth = cellDefaultWidth;
            }
            newWidths[id] = Math.min(Math.max(maxContentWidth, minWidth), maxWidth);
        }
        setColumnWidths(newWidths);
        setIsMeasuring(false);
    }, 100), []);
    useDeepCompareEffect(() => {
        if (!sampledRows.length) {
            return;
        }
        calculateWidths.cancel();
        calculateWidths({
            columnSizing,
            columns,
            measureCellWidth,
            sampledRows,
            tableInstance,
            ...options,
        });
    }, [columnSizing, sampledRows]);
    const columnsWithAutoSizes = React.useMemo(() => setColumnAutoSizes(columns, columnWidths, columnSizing), [columns, columnWidths, columnSizing]);
    return { columnWidths, setTableInstance, columnsWithAutoSizes, isMeasuring };
}
//# sourceMappingURL=useColumnsAutoSize.js.map
