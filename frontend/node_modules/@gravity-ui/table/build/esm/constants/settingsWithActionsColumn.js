import { getActionsColumn } from "./actionsColumn.js";
import { getSettingsColumn } from "./settingsColumn.js";
export const SETTINGS_WITH_ACTIONS_COLUMN_ID = '_settings-with-actions';
const SETTINGS_WITH_ACTIONS_COLUMN_SIZE = 44;
export const getSettingsWithActionsColumn = (columnId = SETTINGS_WITH_ACTIONS_COLUMN_ID, options) => {
    const { cell } = getActionsColumn(columnId, options.actions);
    const { header, meta } = getSettingsColumn(columnId, options.settings);
    return {
        id: columnId,
        size: SETTINGS_WITH_ACTIONS_COLUMN_SIZE,
        minSize: SETTINGS_WITH_ACTIONS_COLUMN_SIZE,
        cell,
        header,
        meta,
    };
};
//# sourceMappingURL=settingsWithActionsColumn.js.map
