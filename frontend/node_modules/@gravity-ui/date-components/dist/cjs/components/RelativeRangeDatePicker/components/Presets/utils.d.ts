import type { DateTime } from '@gravity-ui/date-utils';
import type { ExtractFunctionType } from "../../../types/index.js";
import type { Preset } from "./defaultPresets.js";
import { i18n } from "./i18n/index.js";
export declare function getPresetTitle(start: string, end: string, presets?: Preset[], t?: ExtractFunctionType<typeof i18n>): import("react").ReactNode;
export declare function filterPresets(presets: Preset[], minValue?: DateTime): Preset[];
export interface PresetTab {
    id: string;
    title: string;
    presets: Preset[];
}
export declare function getDefaultPresetTabs({ withTime, minValue, t, }: {
    minValue?: DateTime;
    withTime?: boolean;
    t?: (key: 'Main' | 'Other') => string;
}): PresetTab[];
export declare function filterPresetTabs(tabs: PresetTab[], { minValue }?: {
    minValue?: DateTime;
}): PresetTab[];
