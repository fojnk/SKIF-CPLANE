import type { PopupProps } from '@gravity-ui/uikit';
import type { PopupStyleProps } from "../../../types/index.js";
import type { PickerFormProps } from "./PickerForm.js";
import "./PickerDialog.css";
export interface PickerDialogProps extends PickerFormProps, PopupStyleProps {
    /** Popup anchor */
    anchor: HTMLElement | null;
    /** Manages `Popup` visibility */
    open: boolean;
    /** Handles popup close event */
    onClose: (reason?: Parameters<NonNullable<PopupProps['onOpenChange']>>[2] | 'apply') => void;
    /** Handles popup remove from DOM event */
    onRemove?: () => void;
    /** If true `Popup` act like a modal dialog */
    modal?: boolean;
    /** Element which focus should be returned to */
    returnFocus?: PopupProps['returnFocus'];
    /** If true `Sheet` is used instead of `Popup`  */
    isMobile?: boolean;
}
export declare function PickerDialog({ open, onClose, isMobile, anchor, popupClassName, popupStyle, popupPlacement, popupOffset, modal, returnFocus, onRemove, ...props }: PickerDialogProps): import("react/jsx-runtime").JSX.Element;
