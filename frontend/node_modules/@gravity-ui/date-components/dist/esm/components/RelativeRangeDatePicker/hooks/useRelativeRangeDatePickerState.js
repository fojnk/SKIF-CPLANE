import React from 'react';
import { useControlledState } from '@gravity-ui/uikit';
import { i18n } from "../../utils/validation/i18n/index.js";
import { getRangeValidationResult } from "../../utils/validation/relativeRangeDatePicker.js";
export function useRelativeRangeDatePickerState(props) {
    var _a, _b;
    const [value, setValue] = useControlledState(props.value, (_a = props.defaultValue) !== null && _a !== void 0 ? _a : null);
    const [timeZone, setTimeZone] = useControlledState(props.timeZone, (_b = props.defaultTimeZone) !== null && _b !== void 0 ? _b : 'default', props.onUpdateTimeZone);
    const { t } = i18n.useTranslation();
    const validation = React.useMemo(() => getRangeValidationResult(value, props.allowNullableValues, props.minValue, props.maxValue, props.isDateUnavailable, timeZone, t), [
        value,
        props.allowNullableValues,
        props.isDateUnavailable,
        props.maxValue,
        props.minValue,
        timeZone,
        t,
    ]);
    return Object.assign({ value,
        timeZone,
        setValue(v, tz) {
            var _a;
            setValue(v);
            setTimeZone(tz);
            if (value !== v || (value && timeZone !== tz)) {
                (_a = props.onUpdate) === null || _a === void 0 ? void 0 : _a.call(props, v, tz);
            }
        } }, validation);
}
//# sourceMappingURL=useRelativeRangeDatePickerState.js.map
