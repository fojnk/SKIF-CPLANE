import { useDateFieldState } from "../../DateField/index.js";
import { adjustDateToFormat } from "../../DateField/utils.js";
import { createPlaceholderValue, mergeDateTime } from "../../utils/dates.js";
import { getDateTimeValue } from "../utils/index.js";
import { datePickerStateFactory } from "./datePickerStateFactory.js";
export const useDatePickerState = datePickerStateFactory({
    getPlaceholderTime,
    mergeDateTime,
    setTimezone: (date, timeZone) => date.timeZone(timeZone),
    getDateTime: getDateTimeValue,
    useDateFieldState,
    adjustDateToFormat,
});
function getPlaceholderTime(placeholderValue, timeZone) {
    return createPlaceholderValue({ placeholderValue, timeZone });
}
//# sourceMappingURL=useDatePickerState.js.map
