export function getCalendarModes(formatInfo) {
    if (!formatInfo.hasDate) {
        return undefined;
    }
    const modes = { years: true };
    if (formatInfo.availableUnits.day) {
        modes.days = true;
        modes.months = true;
    }
    if (formatInfo.availableUnits.month) {
        modes.months = true;
    }
    if (formatInfo.availableUnits.quarter && !modes.months) {
        modes.quarters = true;
    }
    return modes;
}
//# sourceMappingURL=getCalendarModes.js.map
