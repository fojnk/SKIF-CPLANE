'use client';
import { jsx as _jsx } from "react/jsx-runtime";
import { dateTime } from '@gravity-ui/date-utils';
import { block } from "../../utils/cn.js";
import { createPlaceholderValue, mergeDateTime } from "../utils/dates.js";
import "./MobileCalendar.css";
const b = block('mobile-calendar');
export function MobileCalendar({ props, state }) {
    var _a, _b;
    let type = 'date';
    if (state.formatInfo.hasTime && state.formatInfo.hasDate) {
        type = 'datetime-local';
    }
    else if (state.formatInfo.hasTime) {
        type = 'time';
    }
    return (_jsx("input", { className: b(), disabled: props.disabled, type: type, value: formatNative(state.dateFieldState.value, type), id: props.id, min: formatNative((_a = props.minValue) === null || _a === void 0 ? void 0 : _a.timeZone(state.timeZone), type), max: formatNative((_b = props.maxValue) === null || _b === void 0 ? void 0 : _b.timeZone(state.timeZone), type), tabIndex: -1, onChange: (e) => {
            var _a, _b;
            if (props.readOnly) {
                return;
            }
            const newValue = e.target.value;
            if (newValue) {
                const localDate = dateTime({
                    input: newValue,
                    format: getDateFormat(type),
                    timeZone: 'system',
                }).timeZone(state.timeZone, true);
                let newDate = state.formatInfo.hasDate
                    ? localDate
                    : createPlaceholderValue({
                        placeholderValue: (_a = props.placeholderValue) === null || _a === void 0 ? void 0 : _a.timeZone(state.timeZone),
                        timeZone: state.timeZone,
                    });
                if (state.formatInfo.hasTime) {
                    newDate = mergeDateTime(newDate, localDate);
                }
                else if (state.value) {
                    newDate = mergeDateTime(newDate, state.value.timeZone(state.timeZone));
                }
                else {
                    newDate = mergeDateTime(newDate, createPlaceholderValue({
                        placeholderValue: (_b = props.placeholderValue) === null || _b === void 0 ? void 0 : _b.timeZone(state.timeZone),
                        timeZone: state.timeZone,
                    }));
                }
                state.setValue(newDate);
            }
            else {
                state.setValue(null);
            }
        } }));
}
function getDateFormat(type) {
    switch (type) {
        case 'time': {
            return 'HH:mm';
        }
        case 'datetime-local': {
            return 'YYYY-MM-DDTHH:mm';
        }
        default: {
            return 'YYYY-MM-DD';
        }
    }
}
function formatNative(date, type) {
    if (!date) {
        return '';
    }
    const format = getDateFormat(type);
    return date.format(format);
}
//# sourceMappingURL=MobileCalendar.js.map
