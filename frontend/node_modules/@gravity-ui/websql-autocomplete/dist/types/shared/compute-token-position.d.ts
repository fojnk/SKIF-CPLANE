import { ParseTree, TokenStream } from 'antlr4ng';
import { CursorPosition } from './autocomplete-types';
type TokenContext = {
    index: number;
    context: ParseTree;
    text: string;
};
export declare function computeTokenContext(parseTree: ParseTree, tokenStream: TokenStream, cursorPosition: CursorPosition, whitespaceTokenTypes?: number[]): TokenContext | undefined;
export {};
