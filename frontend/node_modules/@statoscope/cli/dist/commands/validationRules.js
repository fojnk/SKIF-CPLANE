"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const path_1 = __importDefault(require("path"));
const stats_validator_1 = __importDefault(require("@statoscope/stats-validator"));
const config_1 = require("@statoscope/config");
function default_1(yargs) {
    return yargs.command('vrules', `Show available validation rules base on plugins in statoscope.config.js`, (yargs) => {
        return yargs.option('config', {
            describe: 'path to statoscope config',
            alias: 'c',
            type: 'string',
        });
    }, async (argv) => {
        var _a, _b, _c;
        const { path: configPath, config } = (0, config_1.requireConfig)(argv.config);
        const rootPath = path_1.default.dirname(configPath);
        const validateConfig = (_a = config.validate) !== null && _a !== void 0 ? _a : { rules: {} };
        const validator = new stats_validator_1.default(validateConfig, rootPath);
        let foundRules = false;
        for (const pluginConfig of Object.values((_b = validator.plugins) !== null && _b !== void 0 ? _b : {})) {
            for (const alias of pluginConfig.aliases) {
                const rules = Object.keys((_c = pluginConfig.rules) !== null && _c !== void 0 ? _c : {});
                if (rules.length) {
                    console.log(`Provided from ${alias}/*`);
                    foundRules = true;
                    for (const rule of rules) {
                        console.log(`    ${alias}/${rule}`);
                    }
                    console.log();
                }
            }
        }
        if (!foundRules) {
            console.log('Rules not found');
        }
    });
}
exports.default = default_1;
//# sourceMappingURL=validationRules.js.map