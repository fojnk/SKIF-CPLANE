"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const fs_1 = __importDefault(require("fs"));
const json_ext_1 = require("@discoveryjs/json-ext");
const utils_1 = require("@statoscope/report-writer/dist/utils");
const utils_2 = require("../utils");
function default_1(yargs) {
    return yargs.command('inject-extension', `Add an extension into existing stats`, (yargs) => {
        return yargs
            .option('input', {
            describe: 'path to a stats.json',
            alias: 'i',
            type: 'string',
        })
            .option('extension', {
            describe: 'path to json-file with extension (raw json could be passed as stdin)',
            alias: 'e',
            type: 'string',
        })
            .array(['extension'])
            .demandOption('input');
    }, async (argv) => {
        var _a, _b;
        const extensions = [];
        const parsedExtension = (_b = (_a = argv.extension) === null || _a === void 0 ? void 0 : _a.map((filepath) => JSON.parse(fs_1.default.readFileSync(filepath, 'utf-8')))) !== null && _b !== void 0 ? _b : [];
        if (parsedExtension.length) {
            extensions.push(...parsedExtension.flat());
        }
        else {
            const stdinReports = [
                JSON.parse(fs_1.default.readFileSync(0, 'utf-8') || '[]'),
            ];
            extensions.push(...stdinReports.flat());
        }
        const parsed = await (0, json_ext_1.parseChunked)(fs_1.default.createReadStream(argv.input));
        const merged = (0, utils_2.mergeCustomExtensionsIntoCompilation)(parsed, extensions);
        const outputStream = (0, json_ext_1.stringifyStream)(merged);
        outputStream.pipe(process.stdout);
        await (0, utils_1.waitFinished)(outputStream);
    });
}
exports.default = default_1;
//# sourceMappingURL=injectExtension.js.map