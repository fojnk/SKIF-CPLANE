"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const open_1 = __importDefault(require("open"));
const config_1 = require("@statoscope/config");
const utils_1 = require("../utils");
function default_1(yargs) {
    return yargs.command('generate [input]', `Generate HTML report from JSON-stats
Examples:
Single stats: generate path/to/stats.json --output path/to/report.html
Multiple stats: generate --input path/to/stats-1.json path/to/stats-2.json --output path/to/report.html`, (yargs) => {
        return yargs
            .positional('input', {
            describe: 'path to a current stats.json',
            alias: 'i',
            type: 'string',
        })
            .option('reference', {
            describe: 'path to stats.json to diff with (e.g. master-branch stats)',
            alias: 'r',
            type: 'string',
        })
            .option('config', {
            describe: 'path to statoscope config',
            alias: 'c',
            type: 'string',
        })
            .option('custom-report', {
            describe: 'path to json-file(s) with custom user report(s)',
            type: 'string',
        })
            .positional('output', {
            describe: 'path to a report.html',
            alias: 't',
            type: 'string',
        })
            .option('open', {
            describe: 'open report after done',
            alias: 'o',
        })
            .option('compression', {
            describe: 'use report compression',
            type: 'boolean',
            default: true,
        })
            .array(['input', 'custom-report'])
            .demandOption('input');
    }, async (argv) => {
        argv.output = (0, utils_1.createDestStatReportPath)(argv.input, argv.output);
        const files = [];
        if (argv.reference) {
            if (argv.input.length > 1) {
                console.log(`When reference arg is specified then only first import is used`);
            }
            files.push({ name: argv.input[0], as: 'input.json' });
            files.push({ name: argv.reference, as: 'reference.json' });
        }
        else {
            files.push(...argv.input);
        }
        const { config } = (0, config_1.requireConfig)(argv.config);
        const customReports = (0, utils_1.combineCustomReports)(config, argv['custom-report']);
        console.log(`Generating Statoscope report to ${argv.output} ...`);
        await (0, utils_1.transform)(files, argv.output, customReports, argv.compression);
        console.log(`Statoscope report saved to ${argv.output}`);
        if (argv.open) {
            (0, open_1.default)(argv.output);
        }
    });
}
exports.default = default_1;
//# sourceMappingURL=generate.js.map