import type { Webpack } from '@statoscope/webpack-model/webpack';
import { Report } from '@statoscope/types/types/custom-report';
import { Config } from '@statoscope/types/types/config';
import { Extension } from '@statoscope/stats/spec/extension';
export type TransformFrom = {
    name: string;
    as: string;
};
export declare function mergeCustomReportsIntoCompilation(parsed: Webpack.Compilation, reports: unknown[]): Webpack.Compilation;
export declare function mergeCustomExtensionsIntoCompilation(parsed: Webpack.Compilation, extensions: Extension<unknown>[]): Webpack.Compilation;
export declare function transform(from: Array<TransformFrom | string>, to: string, customReports: Report<unknown, unknown>[] | undefined, compression: boolean): Promise<string>;
export declare const createDestStatReportPath: (from: string[], to?: string) => string;
/**
 * Return true for objects with properties expected from custom report.
 * Type guard.
 */
export declare function isCustomReport(report: unknown): report is Report<unknown, unknown>;
/**
 * Combine custom user reports from statoscope config and command arguments
 * (report json file paths).
 */
export declare function combineCustomReports(config: Config, reportArg?: string[]): Report<unknown, unknown>[];
