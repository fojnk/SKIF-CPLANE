import { Config } from '@statoscope/types/types/config';
export type RequiredConfig = {
    path: string;
    config: Config;
};
export declare function requireConfig(filename?: string): RequiredConfig;
